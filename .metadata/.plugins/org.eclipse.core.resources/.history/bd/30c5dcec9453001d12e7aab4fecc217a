server:
  port: 8888
spring:
  application:
    name: config-server
  cloud: 
    config:
      server:
        git:
          url:https://github.com/ismaelpe/bootCamp-ConfigServer.git
        default-label: main
        search-paths:
        - '*service*'
