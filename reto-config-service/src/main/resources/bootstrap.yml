server:
  port: 8085
spring:
  config:
     import: configserver:http://localhost:8888
  application:
     name: myproduct
  cloud:
     config:
        enable: true
        uri: http://localhost:8888
